# AGENTS.md

本文件适用于整个“铭深文件生成系统”项目。项目正式根目录为
`D:\Codes\DGM`。Codex 在开始任何修改前必须完整阅读本文件。

## 必读顺序

1. `AGENTS.md`
2. `README.md`
3. `docs/COMMUNICATION_WORKFLOW.md`
4. `docs/BUSINESS_RULES.md`
5. `docs/SYSTEM_DESIGN.md`
6. `docs/IMPLEMENTATION_PLAN.md`
7. 当前 GitHub Issue 和对应阶段任务文件，例如 `docs/INSTRUCTIONS_PHASE_2.md`
8. `docs/ACCEPTANCE_CRITERIA.md`

如内容冲突，优先级依次为：用户在当前任务中的最新明确指令、当前 GitHub Issue 的验收条件、`AGENTS.md`、其他项目文档。Codex 必须在完成报告中说明差异。

## 项目目标

系统用于拆解厂车辆资料整理。工作人员批量上传车辆外观照片、行驶证、微信截图等资料；系统在后台执行 OCR，按车牌号和 VIN 合并同一车辆的信息，并生成每批次表格和持续更新的车辆总表。

系统部署在一台配有 RTX 3090 的 Windows 电脑上，通过厂内局域网访问。业务图片、数据库和导出文件默认只保存在本机。

## 当前阶段

第一阶段已由 PR #3 完成并合并。当前只实施
`docs/INSTRUCTIONS_PHASE_2.md`：

- 浏览器按数量和总字节限制自动分组上传；
- 逐文件 JSON 结果、可靠进度和有限自动重试；
- 上传失败记录、刷新恢复及批次图片分页；
- 向已有批次补充图片；
- 自动化测试、真实浏览器验证和 500 张合成图片验证。

当前阶段禁止提前实现 GPU OCR、常驻 OCR Worker、车辆匹配重写、批次 Excel
或最新总表。

## 开始工作前

- 本机正式部署目录是 `D:\Codes\DGM`；GitHub/Codex 临时工作区允许位于其他路径。
- 确认当前目录是本仓库根目录，且正在处理明确的 GitHub Issue。
- 检查仓库状态和用户已有修改。
- 查找并遵守更深层目录中的 `AGENTS.md`。
- 阅读相关代码、测试和上述项目文档。
- 从最新 `main` 创建独立分支，分支名使用 `codex/issue-编号-简短说明`。
- 给出简短计划后继续实施；不要只报告计划。
- 如项目根目录为空，先报告实际情况，再按文档建立最小项目结构。

## 数据安全

- 不得删除、覆盖或重新创建真实 `data\vehicles.db`。
- 不得删除 `data\uploads`、`data\backups`、`exports` 或用户文件。
- 数据库变更必须通过可重复执行的版本化迁移完成。
- 测试必须使用临时数据库和临时图片目录。
- 禁止运行 `git reset --hard`、`git clean -fd`、批量删除或其他破坏性命令。
- 遇到旧数据库结构异常时先停止写入并报告，不得用空数据库替代。

## 技术约束

- 运行平台：Windows 10/11。
- Web：Flask；生产运行使用 Waitress。
- 数据库：SQLite，启用外键、WAL 和 busy timeout。
- OCR：未来使用 RTX 3090；当前阶段不得加载 OCR。
- 所有路径必须从 `Path(__file__)` 推导，禁止硬编码完整盘符路径。
- SQL 必须参数化。
- 图片使用 SHA-256 全局去重。
- 一张图片失败不能导致整个批次失败。
- 长耗时任务不得在 Web 请求线程中执行。
- 不使用 Redis、Celery、PostgreSQL、Docker 或云服务，除非用户批准新的架构阶段。

## 业务约束

- 标准导出仅包含 `docs/BUSINESS_RULES.md` 中规定的 14 个字段。
- 缺失值在数据库中保存为空，显示和导出时才写“暂无”。
- 车牌号是主要车辆键，VIN 是辅助键。
- 不确定的图片进入待确认；不得根据上传顺序强行串车。
- 冲突值不得静默覆盖，必须保留并交由人工复核。
- 同一图片可以出现在多个批次，但只保存一份证据和一份 OCR 结果。

## 修改原则

- 优先小范围、可审查的修改。
- 保留现有接口和已有功能；确需破坏性变更时先说明兼容方案。
- 不擅自扩大当前任务范围。
- 对迁移、状态机、任务领取和重复图片规则添加必要注释。
- 页面文字使用简洁中文，项目名称统一为“铭深文件生成系统”。
- 不因测试失败而删除测试或降低验收标准。
- 不直接向 `main` 提交；每个 Issue 使用独立分支和 Pull Request。
- Pull Request 必须引用对应 Issue，并使用 `Closes #编号` 自动关闭已完成任务。

## 验证要求

每次完成代码修改后至少执行：

- 全部自动化测试；
- Python 编译检查；
- 数据库迁移重复执行测试；
- 相关页面或接口的最小运行验证。

涉及上传时，必须证明 Web 请求没有调用 OCR；涉及任务领取时，必须验证同一任务不能被重复领取。

## 完成报告

完成后必须说明：

1. 实现内容；
2. 修改文件；
3. 数据库迁移；
4. 数据安全措施；
5. 测试命令和结果；
6. 尚未实现的阶段；
7. 已知限制和需要用户确认的问题。

不得只回复“已完成”。
