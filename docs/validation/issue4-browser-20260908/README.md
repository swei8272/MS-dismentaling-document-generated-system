# 真实浏览器合成验收证据

最终应用验证提交：`7b71407bed03eda7a65e4e7803267fba956cd213`。
完整中文操作、环境、命令和口径见 [验证报告](../../PHASE_2_VALIDATION_REPORT.md)。

- `verified_*`：最终 7b71407 提交，全新隔离数据库的 500 张实测；最终截图 30。
- `final_*`：同日 e54f7bb 时的第一次全新增 500 张；此时应用代码仍为 0acd823，
  文件名中的 final 是当次计划名称，不代表本轮最终提交。
- `raw_*`、`synthetic_events.jsonl`：e54f7bb 的综合故障场景及第一次
  420 added / 80 reused 的 500 张；不得把三次性能数据混用。
- `raw_checks.json`：e54f7bb 的 59 pytest / 16 JS；`verified_checks.json`：
  最终代码 60 pytest / 17 JS。历史检查保留，不覆盖。
- 截图 01–12：51/52 分页、确认态、503 重试、列表错误、响应抑制、字节边界、失败分页。
- 截图 13 是最后一条替换尚在绘制的中间帧；截图 11 是分页中间帧。
  它们的文件名过早描述了预期，**不作为最终通过证据**。
  实际失败第二页使用 12；26 条全部解决使用 29 和 `final_26_dom.json`。
- 截图 14–25：超限替换、待同步/已保存、刷新、连接拒绝/重启恢复、延迟分页、混合损坏图。
- 截图 26–28：徽标修复前的全新增规模运行；29：26 条恢复独立终态复核；
  30：最终版本 500 张及状态徽标更新。
- `replacement-actions.json` 只记录控制连接恢复后的最后 18 条动作；完整 26 条
  请求存在服务端事件中。不得将它宣称为完整的 26 步浏览器事件日志。

`summarize.py` 只接受带专用合成标记的运行根，SQLite 以 mode=ro 打开。默认读取
综合数据库；`--final` 复算徽标修复前全新增运行；`--verified` 复算最终运行。
脚本输出不包括完整本机路径、真实业务资料、浏览器标题或进程命令行。

服务端峰值来自 Windows PeakWorkingSetSize；浏览器值是全 Chrome 进程每秒
WorkingSet64/PrivateMemorySize64 求和的采样最大值，包含其他既有标签和扩展，
不能归因到单独测试标签，也不是生命周期峰值。浏览器响应抑制夹具返回提交后 503，
不冒称 TCP 静默丢包。实际连接拒绝由停止本次隔离 Waitress 产生。

服务已停止，5057/5058 监听数为 0。测试数据库与图片仍保留在本机临时目录，
未提交数据库或合成源图片，只提交应用截图和无业务数据的测量摘要。
